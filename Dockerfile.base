# Base image with Bun runtime
FROM oven/bun:1.2.21-slim AS base
WORKDIR /app

# Install Python for transcript worker
RUN apt-get update && apt-get install -y \
    python3 \
    python3-pip \
    python3-venv \
    ffmpeg \
    && rm -rf /var/lib/apt/lists/*